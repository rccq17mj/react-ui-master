module.exports = (content) => {
  reg = /<Example>[\s\S]*?<\/Example>/g;
  content = content.replace(reg, (exam) => {
    var code = '<Code>{`' + exam.replace(/\s*<\/?Example>\n?/g, '').replace(/`/g, '\\`') + '`}</Code>';
    exam += code;
    return exam;
  });
  return content;
};
